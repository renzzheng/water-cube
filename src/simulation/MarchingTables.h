#pragma once

// edge table - which edges are cut for each of 256 configurations
extern const int edgeTable[256];

// triangle table - which triangles to form for each configuration  
extern const int triTable[256][16];